<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New answer</h1>

  <%= render "form", answer: @answer %>

  <%= link_to 'Back to question', question_path(@question), class: "ml-2 rounded-lg py-2 px-4 bg-gray-100 inline-block font-medium" %>
</div>
